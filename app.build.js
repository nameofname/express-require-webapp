({
    // Directory to all of the assets to be built:
    appDir: "src",
    // Directory where JS modules are kept:
    baseUrl: "js",
    // Directory to store the built files in:
    dir: "build",
    // Modules to build: 
    modules: [
        {
            name: "main"
        }
    ]
})